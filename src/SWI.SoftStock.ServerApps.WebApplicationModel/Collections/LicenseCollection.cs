namespace SWI.SoftStock.ServerApps.WebApplicationModel.Collections
{
	public class LicenseCollection : BaseCollection<LicenseModel>
    {
		public LicenseCollection(string order, string sort): base(order, sort)
        {
        }
    }
}